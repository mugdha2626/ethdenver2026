# ConfidentialConnect — DevNet Configuration
# Copy to .env when connecting to Canton DevNet via SSH tunnel
#
# Setup:
#   1. Run deploy-devnet.sh on the DevNet machine
#   2. SSH tunnel: ssh -L 7575:localhost:7575 -N dev5@136.112.241.18
#   3. cp .env.devnet .env
#   4. cd slack-bot && npm run dev

# SLACK (Required — same tokens as local dev)
SLACK_BOT_TOKEN=xoxb-your-bot-token-here
SLACK_APP_TOKEN=xapp-your-app-token-here

# CANTON API — v2 for DevNet / Canton 3.x
CANTON_API_VERSION=v2
CANTON_JSON_API_URL=http://localhost:7575

# AUTH — unsafe secret matches Canton DevNet default
JWT_SECRET=unsafe

# PACKAGE — must match daml.yaml 'name' field
CANTON_PACKAGE_NAME=confidential-connect

# DAR path (bot uploads on startup)
DAR_PATH=./daml/.daml/dist/confidential-connect-0.1.0.dar

# DATABASE
DB_PATH=./data/cc.db
